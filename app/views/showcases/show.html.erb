<div class="page-header">
  <div class="pull-right">
    <%= link_to t('.edit', :default => t("helpers.links.edit")),
      edit_exhibit_showcase_path(@exhibit, @showcase), :class => 'btn btn-default' %>
  </div>

  <h1 class="ui-header"><%= @exhibit.title %> > <%= @showcase.title %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <%= @showcase.description.html_safe %>
    <p><%= link_to t('.sections'), exhibit_showcase_sections_path(@exhibit, @showcase) %></p>
  </div>
</div>
